
# OS
# Python
*.egg-info/
*.py[cod]
.DS_Store
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
__pycache__/

# Runtime data and the 82MB raw timetable are ignored; the small computed
# corridor cache is committed so a fresh clone runs without the raw download.
data/*
!data/corridor.json
# Sample TMS/SMMS/TDMS/COA export files, so `sih --from-feeds` runs after clone
!data/feeds
data/feeds/*
!data/feeds/*.csv

# Stale one-off export
dashboard_imported.html

Automatic_Block_Planning_Documentation.pdf
